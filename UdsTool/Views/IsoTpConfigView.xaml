<UserControl x:Class="UdsTool.Views.IsoTpConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UdsTool.Views"
             xmlns:converters="clr-namespace:UdsTool.Utils.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="ISO-TP Configuration" FontWeight="Bold" FontSize="16" Margin="10"/>
            <Button Content="Apply" Command="{Binding ApplyConfigCommand}" Margin="10,0" Padding="10,2"/>
            <Button Content="Reset to Defaults" Command="{Binding ResetToDefaultsCommand}" Margin="0" Padding="10,2"/>
        </StackPanel>

        <!-- Configuration Panel -->
        <ScrollViewer Grid.Row="1" Margin="10">
            <StackPanel>
                <GroupBox Header="CAN IDs" Margin="0,5,0,15">
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Request CAN ID (hex):" VerticalAlignment="Center" Margin="0,5"/>
                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding Config.RequestCanId, StringFormat=X3}" Width="100" Margin="5"/>
                            <TextBlock Text="Standard: 0x7E0, Extended: 0x18DA00F1" Opacity="0.7" VerticalAlignment="Center" Margin="10,0"/>
                        </StackPanel>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Response CAN ID (hex):" VerticalAlignment="Center" Margin="0,5"/>
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding Config.ResponseCanId, StringFormat=X3}" Width="100" Margin="5"/>
                            <TextBlock Text="Standard: 0x7E8, Extended: 0x18DAF100" Opacity="0.7" VerticalAlignment="Center" Margin="10,0"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Flow Control Parameters" Margin="0,5,0,15">
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Block Size (BS):" VerticalAlignment="Center" Margin="0,5"/>
                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding Config.BlockSize}" Width="100" Margin="5"/>
                            <TextBlock Text="0 = no limit, typical values: 0, 8, 16" Opacity="0.7" VerticalAlignment="Center" Margin="10,0"/>
                        </StackPanel>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Separation Time (STmin):" VerticalAlignment="Center" Margin="0,5"/>
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding Config.SeparationTime}" Width="100" Margin="5"/>
                            <TextBlock Text="Minimum time between frames in ms" Opacity="0.7" VerticalAlignment="Center" Margin="10,0"/>
                        </StackPanel>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Timeout (ms):" VerticalAlignment="Center" Margin="0,5"/>
                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding Config.Timeout}" Width="100" Margin="5"/>
                            <TextBlock Text="Timeout for UDS responses, typical value: 1000ms" Opacity="0.7" VerticalAlignment="Center" Margin="10,0"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Options" Margin="0,5,0,15">
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Padding Enabled:" VerticalAlignment="Center" Margin="0,5"/>
                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding Config.PaddingEnabled}" VerticalAlignment="Center" Margin="5"/>
                            <TextBlock Text="Pad CAN frames to full length (usually required)" Opacity="0.7" VerticalAlignment="Center" Margin="10,0"/>
                        </StackPanel>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Extended Addressing:" VerticalAlignment="Center" Margin="0,5"/>
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding Config.ExtendedAddressing}" VerticalAlignment="Center" Margin="5"/>
                            <TextBlock Text="Use ISO-TP extended addressing" Opacity="0.7" VerticalAlignment="Center" Margin="10,0"/>
                        </StackPanel>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Use CAN FD:" VerticalAlignment="Center" Margin="0,5"/>
                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding Config.UseCanFd}" VerticalAlignment="Center" Margin="5"/>
                            <TextBlock Text="Use CAN FD for larger payloads" Opacity="0.7" VerticalAlignment="Center" Margin="10,0"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Load" Command="{Binding LoadConfigCommand}" Margin="5" Padding="10,3"/>
            <Button Content="Save" Command="{Binding SaveConfigCommand}" Margin="5" Padding="10,3"/>
        </StackPanel>
    </Grid>
</UserControl>