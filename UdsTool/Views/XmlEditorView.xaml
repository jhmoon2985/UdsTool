<UserControl x:Class="UdsTool.Views.XmlEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UdsTool.Views"
             xmlns:behaviors="clr-namespace:UdsTool.Behaviors"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:converters="clr-namespace:UdsTool.Utils.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0">
            <Button Content="New" Command="{Binding NewDatabaseCommand}" ToolTip="Create new database"/>
            <Button Content="Open" Command="{Binding LoadDatabaseCommand}" ToolTip="Load database from file"/>
            <Button Content="Save" Command="{Binding SaveDatabaseCommand}" ToolTip="Save database"/>
            <Button Content="Save As" Command="{Binding SaveAsDatabaseCommand}" ToolTip="Save database as"/>
            <Separator/>
            <Button Content="Add Service" Command="{Binding AddServiceCommand}" ToolTip="Add new service"/>
            <Button Content="Add Subfunction" Command="{Binding AddSubfunctionCommand}" ToolTip="Add new subfunction"/>
            <Button Content="Add Data ID" Command="{Binding AddDataIdCommand}" ToolTip="Add new Data ID"/>
            <Button Content="Add Data" Command="{Binding AddDataCommand}" ToolTip="Add new data"/>
            <Button Content="Delete" Command="{Binding DeleteElementCommand}" ToolTip="Delete selected item"/>
        </ToolBar>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- TreeView with UDS Elements -->
            <TreeView Grid.Column="0" Margin="5" ItemsSource="{Binding Database.ServiceIds}">
                <b:Interaction.Behaviors>
                    <behaviors:TreeViewSelectedItemBehavior SelectedItem="{Binding SelectedElement, Mode=TwoWay}"/>
                </b:Interaction.Behaviors>

                <TreeView.Resources>
                    <!-- Style for TreeViewItem -->
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="False"/>
                        <b:Interaction.Behaviors>
                            <behaviors:TreeViewItemBehavior SelectedItemChangedCommand="{Binding DataContext.SelectItemCommand, RelativeSource={RelativeSource AncestorType=TreeView}}"/>
                        </b:Interaction.Behaviors>
                    </Style>
                </TreeView.Resources>

                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Subfunctions}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                            <TextBlock Text=" - "/>
                            <TextBlock Text="{Binding HexValue}"/>
                        </StackPanel>

                        <HierarchicalDataTemplate.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Data}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}"/>
                                    <TextBlock Text=" - "/>
                                    <TextBlock Text="{Binding HexValue}"/>
                                </StackPanel>

                                <HierarchicalDataTemplate.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}"/>
                                            <TextBlock Text=": "/>
                                            <TextBlock Text="{Binding Value}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </HierarchicalDataTemplate.ItemTemplate>
                            </HierarchicalDataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <!-- Element Properties -->
            <Border Grid.Column="1" BorderBrush="LightGray" BorderThickness="1" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Element Properties" FontWeight="Bold" Margin="5"/>

                    <ScrollViewer Grid.Row="1" Margin="5">
                        <StackPanel>
                            <!-- Service ID Properties -->
                            <StackPanel Visibility="{Binding SelectedElement, Converter={StaticResource TypeToVisibilityConverter}, ConverterParameter='UdsTool.Core.Models.ServiceId'}">
                                <TextBlock Text="Service ID" FontWeight="Bold" Margin="0,10,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedElement.Name, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedElement.Description, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Value (hex):" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedElement.Value, UpdateSourceTrigger=PropertyChanged, StringFormat=X2}" Margin="5,2"/>
                                </Grid>
                            </StackPanel>

                            <!-- Subfunction Properties -->
                            <StackPanel Visibility="{Binding SelectedElement, Converter={StaticResource TypeToVisibilityConverter}, ConverterParameter='UdsTool.Core.Models.Subfunction'}">
                                <TextBlock Text="Subfunction" FontWeight="Bold" Margin="0,10,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedElement.Name, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedElement.Description, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Value (hex):" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedElement.Value, UpdateSourceTrigger=PropertyChanged, StringFormat=X2}" Margin="5,2"/>
                                </Grid>
                            </StackPanel>

                            <!-- Data ID Properties -->
                            <StackPanel Visibility="{Binding SelectedElement, Converter={StaticResource TypeToVisibilityConverter}, ConverterParameter='UdsTool.Core.Models.DataId'}">
                                <TextBlock Text="Data ID" FontWeight="Bold" Margin="0,10,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedElement.Name, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedElement.Description, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Value (hex):" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedElement.Value, UpdateSourceTrigger=PropertyChanged, StringFormat=X2}" Margin="5,2"/>
                                </Grid>
                            </StackPanel>

                            <!-- Data Properties -->
                            <StackPanel Visibility="{Binding SelectedElement, Converter={StaticResource TypeToVisibilityConverter}, ConverterParameter='UdsTool.Core.Models.UdsData'}">
                                <TextBlock Text="Data" FontWeight="Bold" Margin="0,10,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedElement.Name, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Value:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedElement.Value, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>
                                </Grid>
                            </StackPanel>

                            <!-- No element selected -->
                            <TextBlock Text="Select an element to edit its properties" 
                                       Visibility="{Binding SelectedElement, Converter={StaticResource NullToVisibilityConverter}}"
                                       Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>